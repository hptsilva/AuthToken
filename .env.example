# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO BANCO DE DADOS
# -----------------------------------------------------------------------------

# Tipo de conexão com o banco de dados. Opções: "mysql", "mariadb", "sqlite".
AUTHTOKEN_DB_CONNECTION=mysql

# --- Configurações para MySQL/MariaDB ---
# Insira os detalhes abaixo apenas se AUTHTOKEN_DB_CONNECTION for "mysql" ou "mariadb".

# Host do banco de dados (ex: localhost, 127.0.0.1).
AUTHTOKEN_DB_HOSTNAME=localhost
# Nome do banco de dados.
AUTHTOKEN_DB_DATABASE=auth_token
# Usuário do banco de dados.
AUTHTOKEN_DB_USER=root
# Senha do usuário do banco de dados.
AUTHTOKEN_DB_PASSWORD=secret

# --- Configuração para SQLite ---
# Insira o caminho abaixo apenas se AUTHTOKEN_DB_CONNECTION for "sqlite".

# Caminho para o diretório onde o arquivo do banco de dados SQLite será salvo.
# Se deixado em branco, o padrão será o diretório raiz do projeto.
AUTHTOKEN_DB_SQLITE_PATH=

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO TOKEN
# -----------------------------------------------------------------------------

# Tipo de dado para a coluna "user_id" na tabela de refresh tokens.
# Use "INT" para IDs numéricos ou "VARCHAR(255)" para UUIDs ou strings.
AUTHTOKEN_USER_TYPE=INT

# Chave secreta para assinar os Access Tokens (JWTs).
# Gere uma chave segura executando: php ./vendor/bin/auth-token secret
AUTHTOKEN_APP_SECRET=

# Tempo de vida do Access Token. Formato de intervalo do PHP DateInterval.
# Exemplos: '+15 minutes', '+1 hour', '+30 seconds'.
AUTHTOKEN_ACCESS_TOKEN_TIMEOUT='+15 minutes'

# Tempo de vida do Refresh Token. Formato de intervalo do PHP DateInterval.
# Exemplo: 'P7D' (7 dias), 'P1M' (1 mês).
AUTHTOKEN_REFRESH_TOKEN_INTERVAL='P7D'
